package Assignments.Lab4;
public class Q2 {

    public int SizeAlter(){
        int size=0;
        if (tail==null) return size;
        else size++;
        Node<E> node=tail;
        while (node.getNext()!=tail){
            size++;
            node=node.getNext();
        }
        return s;
    }
    }

